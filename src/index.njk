---
layout: layouts/base.njk
title: Home
---

<div class="container">
  <section class="intro">
    <h1>Hello, I'm <a href="https://prev.bryantke.com">Bryant</a>.</h1>
    <p class="lead">Some stuff I'm working on.</p>
  </section>

  {% if collections.featuredPosts.length > 0 %}
  <section class="featured-posts">
    <h2>Check this out!</h2>
    <div class="post-list">
      {% for post in collections.featuredPosts | limit(3) %}
      <article class="post-item">
        <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
        <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
        {% if post.data.description %}
        <p>{{ post.data.description }}</p>
        {% endif %}
      </article>
      {% endfor %}
    </div>
  </section>
  {% endif %}

  <section class="recent-posts">
    <h2>Latest</h2>
    <div class="post-list">
      {% for post in collections.posts | limit(5) %}
      <article class="post-item">
        <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
        <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
        {% if post.data.description %}
        <p>{{ post.data.description }}</p>
        {% endif %}
      </article>
      {% endfor %}
    </div>
    <p><a href="/latest/">View all posts →</a></p>
  </section>
</div>