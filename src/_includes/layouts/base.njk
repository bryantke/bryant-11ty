<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} | {% endif %}Bryant Ke</title>
  <meta name="description" content="{{ description or 'Some stuff I\'m working on' }}">

  <!-- Open Graph -->
  <meta property="og:title" content="{{ title or 'Bryant Ke' }}">
  <meta property="og:description" content="{{ description or 'Personal blog about code, design, and ideas.' }}">
  <meta property="og:type" content="{% if tags %}article{% else %}website{% endif %}">

  <link rel="stylesheet" href="/css/styles.css?v=2">
  <link rel="alternate" type="application/rss+xml" title="RSS Feed" href="/feed.xml">
  <script src="/js/theme.js?v=2"></script>
</head>
<body>
  <a href="#main" class="skip-link">Skip to main content</a>

  {% include "partials/header.njk" %}

  <main id="main">
    {{ content | safe }}
  </main>

  {% include "partials/footer.njk" %}
</body>
</html>